{
  "entries": [
    {
      "target": "EXAMPLE",
      # a target research where do you want to change the note

      "stage": 1,
      # number of the stage at which the target note is located, stage numbering starts from 0

      "radius": 3,
      # note field radius, minimum 1 maximum 4

      "blanks": 6,
      # the number of missing cells

      "meta": ["ROUND", "ROUNDEX", "SEPARATED"],

      "aspects": ["victus", "ordo", "herba", "vacuos", "volatus"],
      # a list of aspects that will be in the note, if there are more than 10 of them, then problems may begin!

      "color": "sensus" or 12648384,
      # research note color, accepts aspect and int

      "hash-delta": 1
      # the note card is generated semi randomly, you can shift the seed using this field
    }
  ]
}